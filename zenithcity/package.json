{
  "name": "zenithcity",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "docker:build": "docker-compose build",
    "docker:build:backend": "cd backend && docker build -t zenithcity-backend .",
    "docker:build:frontend": "cd frontend && docker build -t zenithcity-frontend .",
    "docker:up": "docker-compose up -d",
    "docker:up:prod": "docker-compose -f docker-compose.prod.yml up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "mobile:setup": "node mobile-test.js",
    "mobile:dev": "cp frontend/.env.mobile frontend/.env && npm run dev",
    "prod:test": "node test-production.js",
    "prod:build": "cp frontend/.env.production frontend/.env && npm run build",
    "deploy": "git add . && git commit -m 'Deploy to production' && git push origin main"
  }
}
